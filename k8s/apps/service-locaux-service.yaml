apiVersion: v1
kind: Service
metadata:
  name: service-locaux
  namespace: planification
  labels:
    app: service-locaux
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  selector:
    app: service-locaux
