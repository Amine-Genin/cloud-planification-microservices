apiVersion: v1
kind: Service
metadata:
  name: service-catalogue
  namespace: planification
  labels:
    app: service-catalogue
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  selector:
    app: service-catalogue
