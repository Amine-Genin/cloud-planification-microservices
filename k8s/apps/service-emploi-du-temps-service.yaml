apiVersion: v1
kind: Service
metadata:
  name: service-emploi-du-temps
  namespace: planification
  labels:
    app: service-emploi-du-temps
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  selector:
    app: service-emploi-du-temps
