apiVersion: v1
kind: Service
metadata:
  name: catalogue-db
  namespace: planification
  labels:
    app: catalogue-db
spec:
  type: ClusterIP
  ports:
    - name: mysql
      port: 3306
      targetPort: 3306
  selector:
    app: catalogue-db
  clusterIP: None
