apiVersion: v1
kind: Service
metadata:
  name: emploi-db
  namespace: planification
  labels:
    app: emploi-db
spec:
  type: ClusterIP
  ports:
    - name: mysql
      port: 3306
      targetPort: 3306
  selector:
    app: emploi-db
  clusterIP: None
